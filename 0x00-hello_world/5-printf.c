#include<stdio.h>

/**
 * main - function that uses printf to print a set of chars
 *
 * Return: 0 if successful.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
